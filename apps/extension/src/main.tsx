import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div className="p-4 w-80">
      <h1 className="text-xl font-bold">ResumeTailor</h1>
      <p>Popup loaded.</p>
    </div>
  </React.StrictMode>,
)
